1、bif -- build in function

2、input() -> return a string type

3、原始字符串 str = r'C:\now' （相当于转义字符）

4、list1 = list2[:] :复制, == list1 = list2.copy()
   list1 = list2 :别名,改变其中一个，另一个同样改变，实际上是同一个东西,相当于C改变地址指向的值

5、文件打开方式：
    r 只能读
    r+可读可写，不会创建不存在的文件。如果直接写文件，则从顶部开始写，覆盖之前此位置的内容，如果先读后写，则会在文件最后追加内容。
    w+ 可读可写 如果文件存在 则首先用空文件覆盖整个文件 不存在则创建
    w 只能写 覆盖整个文件 不存在则创建
    a 只能写 从文件底部添加内容 不存在则创建
    a+ 可读可写 从文件顶部读取内容 从文件底部添加内容 不存在则创建
